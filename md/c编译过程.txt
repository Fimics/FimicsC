1.预处理
   宏定义展开，头文件展开，条件编译，同时把代码中的注释删除，这里并不会检查语法
   gcc -E a.c -o a.i
2.编译
   检查语法，把预处理后文件编译生成汇编文件
   gcc -S a.i -o a.s
3.汇编
    把汇编文件生成目标文件(二进制文件)
    gcc -c a.s -o a.o
4.链接
   c语言写的程序是需要依赖各种库的，所以编译之后还需要把库链接到最终的可执行程序中去
   gcc a.o -o a.exe